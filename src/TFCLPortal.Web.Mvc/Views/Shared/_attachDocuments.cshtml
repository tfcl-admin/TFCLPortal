@model List<TFCLPortal.FilesUploads.Dto.FilesUploadListDto>
<style>
    .modal-title {
        text-align: center;
        width: 100%;
    }
</style>


@if (@Model.Count > 0)
{
    <input type="hidden" id="ApplicationId" value="@Model[0].ApplicationId" />
    <div class="tab_content active" style="padding: 15px 20px">
        <div class="row">
            <div class="col-md-12 text-center ">
                <h4><u>Applicant Personal Documents</u></h4>
            </div>
            @if (Model.Where(x => x.ScreenCode == "applicant_photo").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_photo").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_cnic_front").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_cnic_front").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_cnic_back").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_cnic_back").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "ntn-atl_document").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "ntn-atl_document").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "hh_rent_agreement").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "hh_rent_agreement").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>Dependant fee details</u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "dependent_fee_voucher").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "dependent_fee_voucher").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>applicant business documents</u></h4>
            </div>


            @if (Model.Where(x => x.ScreenCode == "business_main_front_view").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "business_main_front_view").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "business_proof_document").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "business_proof_document").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_school_reg_certification").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_school_reg_certification").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_school_affiliation").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_school_affiliation").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_mou_of_franchise").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_mou_of_franchise").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_school_front_view").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_school_front_view").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_school_inner_view").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_school_inner_view").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_stock_view").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_stock_view").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_rent_agreement").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_rent_agreement").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_partnership_documents").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_partnership_documents").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_others_one").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_others_one").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_others_two").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_others_two").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_others_three").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_others_three").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>collateral document</u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "picture").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "picture").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "ownership_document").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "ownership_document").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "valuation_certificate").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "valuation_certificate").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "collateral_all").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "collateral_all").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "collateral_all_documents").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "collateral_all_documents").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>Business Income</u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "ei_student_strength_(voucher)").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "ei_student_strength_(voucher)").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "ei_student_attendance").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "ei_student_attendance").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "ei_fee_register").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "ei_fee_register").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "ei_fee_voucher").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "ei_fee_voucher").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "sales_voucher").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "sales_voucher").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "academy_student_attendance").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "academy_student_attendance").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "academy_fee_register").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "academy_fee_register").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "academy_fee_voucher").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "academy_fee_voucher").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>Associated Income</u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "canteen_income_document").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "canteen_income_document").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "annual_fund").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "annual_fund").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "copies_income").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "copies_income").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "books_income").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "books_income").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "test_session_income").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "test_session_income").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "uniform_income").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "uniform_income").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "admission_charges").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "admission_charges").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>Other Income</u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "non-associated_income_document").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "non-associated_income_document").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>Business Expense</u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "staff_salary_document").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "staff_salary_document").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "staff_attendance_register").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "staff_attendance_register").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "royalty_fee_voucher").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "royalty_fee_voucher").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "applicant_utility_bil_one").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "applicant_utility_bil_one").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>Household Expense</u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "utility_bill_household").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "utility_bill_household").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>bank statement </u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "bank_statement").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "bank_statement").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>co-applicant documents</u></h4>
            </div>


            @if (Model.Where(x => x.ScreenCode == "co_applicant_photo").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "co_applicant_photo").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "co_applicant_cnic_front").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "co_applicant_cnic_front").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "co_applicant_cnic_back").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "co_applicant_cnic_back").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "other_documents").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "other_documents").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            <div class="col-md-12 text-center">
                <h4><u>guarantor documents</u></h4>
            </div>

            @if (Model.Where(x => x.ScreenCode == "guarantor_photo").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_photo").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_cnic_front").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_cnic_front").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_cnic_back").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_cnic_back").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_salary_slip").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_salary_slip").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_business_related_documents").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_business_related_documents").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_utility_bill").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_utility_bill").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_others_one").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_others_one").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_others_two").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_others_two").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_others_three").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_others_three").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

            @if (Model.Where(x => x.ScreenCode == "guarantor_others_four").Any())
            {
                var items = Model.Where(x => x.ScreenCode == "guarantor_others_four").FirstOrDefault();
                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

                Model.Remove(items);
            }

     
            <div class="col-md-12 text-center">
                <h4><u>Other Documents</u></h4>
            </div>
            @foreach (var items in @Model)
            {

                <div class="col-md-4">

                    <div class="heading text-capitalize">
                        <p class="detailHeading">@items.ScreenCode.Replace("_", " ")  @(items.RespectiveName==null || items.RespectiveName == "" ? "" : "( " +items.RespectiveName+ " )")  @(items.Comments == null || items.Comments == "" ? "" : "( " + items.Comments + " )") </p>
                    </div>
                    <div class="page-form">
                        <div class="form-group">
                            <a href="@Url.Content(items.BaseUrl)" target="_blank" data-rel="prettyPhoto">
                                @if (items.BaseUrl.Contains(".pdf"))
                                {
                                    <img src="~/images/pdfimg.png" alt="Image">
                                }
                                else
                                {
                                    <img src="@Url.Content(items.BaseUrl)" alt="Image">
                                }
                            </a>
                        </div>
                    </div>
                    <div class="row" style="font-size:10px">
                        <div class="col-md-4 text-left"><b>File Type : </b>@System.IO.Path.GetExtension(items.BaseUrl)</div>
                        <div class="col-md-8 text-right"><b>Upload Date : </b> @(items.LastModificationTime==null ? items.CreationTime : items.LastModificationTime)</div>
                    </div>
                </div>

            }



          

            <div class="col-md-12" style="text-align: right;">
                <a id="screenBtnUD" href="" data-toggle="modal" data-target="#_UploadDocuments_approved" class="btn btn-sm btn-outline-success" title="Screen">Screen<span></span></a>
                <a id="discrepentBtnUD" href="" data-toggle="modal" data-target="#_UploadDocuments_discrepant" class="btn btn-sm btn-outline-danger" title="Discrepent">Discrepent<span></span></a>
            </div>
        </div>
    </div>
}
else
{
    <div class="tab_content" style="padding: 15px 20px">
        <div class="row">
            <span style="text-align: center;width: 100%;color: black;font-weight: bold;">Sorry, No Documents found againt this application .</span>
        </div>
    </div>
}
@if(ViewBag.UploadedDocumentsAction!="Hide")
{
    
<!--Approved Modal -->
<div class="modal fade" id="_UploadDocuments_approved" tabindex="-1" role="dialog" style="z-index: 1100;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <div class="modal-title">Approved Confirmation</div>
                <button type="button" class="close" data-dismiss="modal" aria-div="close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Are you sure you want to mark this form Screened?
            </div>
            <div class="modal-footer">
                <button id="btnScreenUD" type="button" class="btn btn-success bg1 ef1">Mark Screened</button>
                <button data-dismiss="modal" type="button" class="btn btn-danger bg1 ef1">Cancel</button>
            </div>
        </div>
    </div>
</div>
<!-- Modal -->
<!--discrepent Modal -->
<div class="modal fade" id="_UploadDocuments_discrepant" tabindex="-1" role="dialog" style="z-index: 1100;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <div class="modal-title">Discrepent Confirmation</div>
                <button type="button" class="close" data-dismiss="modal" aria-div="close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <textarea placeholder="Type Reason" id="commentIdUD"></textarea>
                <span class="text-danger" style="display:none" id="comment_errorUD">this field is required</span>
            </div>
            <div class="modal-footer">
                <button type="button" id="btndiscrepentUD" class="btn btn-success bg1 ef1">Mark Discrepent</button>
                <button type="button" class="btn btn-danger bg1 ef1" data-dismiss="modal">Close</button>
            </div>
        </div>

    </div>
</div>

}

<script>

    $('#commentId').attr('required', 'required');
        //alert('@ViewBag.UploadedDocumentsAction');

    $(document).ready(function () {
        //alert('@ViewBag.PersonalAction');
        btnChange('@ViewBag.UploadedDocumentsAction');
    });


    function btnChange(currentState) {
        //alert(currentState);
        if (currentState == "Screened") {
            $('#screenBtnUD').html("<i class='fa fa-check'></i>  Marked Screened");
            $('#discrepentBtnUD').html("Discrepent");
        }
        else if (currentState == "Discrepent") {
            $('#discrepentBtnUD').html("<i class='fa fa-times'></i> Marked Discrepent");
            $('#screenBtnUD').html("Screen");
        }
        else if (currentState == "Hide") {
            $('#screenBtnUD').toggleClass("d-none");
            $('#discrepentBtnUD').toggleClass("d-none");
        }
    }

    $('#btnScreenUD').click(function () {
        debugger;
        var comment = $('#commentIdUD').val();
        var Id = $('#ApplicationId').val();
            $.ajax({
                    type: 'POST',
                    datatype: "json",
                    contenttype: 'application/json; charset=utf-8',
                    url: '@Url.Content("~/Dashboard/MarkScreened")',
                data: { 'ApplicationId': Id, 'Screen': 'Uploaded Documents'} ,
                success: function (data) {



                    $('.close').click();

                    if (data.result.includes("successfully")) {
                        abp.notify.success(data.result, 'Mark Screened');
                    }
                    else {
                        abp.notify.info(data.result, 'Mark Screened');
                    }

                    btnChange("Screened");


                    },
                    error: function (data) {
                        debugger
                        abp.notify.error(data.result, 'Error');
                     }


                });
    });
    $('#btndiscrepentUD').click(function () {
        debugger;
        if (!$('#commentIdUD').val()) {
            $('#comment_errorUD').css("display", "block");;
            return false;
        } else {
            $('#comment_errorUD').css("display", "none");
        }
        var comment = $('#commentIdUD').val();
        var Id = $('#ApplicationId').val();
            $.ajax({
                    type: 'POST',
                    datatype: "json",
                    contenttype: 'application/json; charset=utf-8',
                    url: '@Url.Content("~/Dashboard/MarkDiscrepent")',
                data: { 'ApplicationId': Id, 'Screen': 'Uploaded Documents','Reason': comment },
                success: function (data) {

                        $('.close').click();

                        if (data.result.includes("successfully")) {
                            abp.notify.success(data.result, 'Mark Discrepent');

                        }
                        else {
                            abp.notify.info(data.result, 'Mark Discrepent');
                    }

                    btnChange("Discrepent");

                    $('#commentIdUD').val("");

                    },
                    error: function (data) {
                        debugger;
                        abp.notify.error(data.result, 'Error');
                    }
                });
        });
</script>